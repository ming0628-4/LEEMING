import Link from "next/link"; import { resources } from "@/lib/resources";
export const metadata = { title: "Admin" };
export default function Admin() { return <main className="shell page"><div className="section-head"><div><p className="eyebrow">ADMIN MODE</p><h1>资源管理</h1><p className="page-lead">维护同一套 Archive 数据。身份验证将在 Sprint 3 接入。</p></div><Link className="primary-button" href="/admin/upload">＋ 上传资源</Link></div><div className="admin-stats"><div><strong>{resources.length}</strong><span>全部资源</span></div><div><strong>{new Set(resources.map(r => r.category)).size}</strong><span>分类</span></div><div><strong>{resources.filter(r => r.updatedAt.startsWith("2026-07")).length}</strong><span>本月更新</span></div></div><div className="table-wrap"><table><thead><tr><th>资源</th><th>分类</th><th>版本</th><th>更新时间</th><th>状态</th></tr></thead><tbody>{resources.map(r => <tr key={r.slug}><td><Link href={`/item/${r.slug}`}>{r.name}</Link></td><td>{r.category}</td><td>{r.version}</td><td>{r.updatedAt}</td><td><span className="status">已发布</span></td></tr>)}</tbody></table></div></main>; }
