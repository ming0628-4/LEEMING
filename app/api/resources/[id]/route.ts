import{eq,sql}from"drizzle-orm";import{ensureResourcesTable,getDb}from"@/db";import{resources}from"@/db/schema";
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){await ensureResourcesTable();const[row]=await getDb().select().from(resources).where(eq(resources.id,Number((await params).id))).limit(1);return row?Response.json({resource:{...row,tags:JSON.parse(row.tags)}}):Response.json({error:"资源不存在"},{status:404})}
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){await ensureResourcesTable();const id=Number((await params).id);const body=await request.json()as Record<string,unknown>;const values:Record<string,unknown>={updatedAt:sql`CURRENT_TIMESTAMP`};for(const key of["name","version","category","description","why","sourceUrl","status"]as const)if(typeof body[key]==="string")values[key]=body[key].trim();if(Array.isArray(body.tags))values.tags=JSON.stringify(body.tags);const[row]=await getDb().update(resources).set(values).where(eq(resources.id,id)).returning();return row?Response.json({resource:row}):Response.json({error:"资源不存在"},{status:404})}
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){await ensureResourcesTable();const[row]=await getDb().delete(resources).where(eq(resources.id,Number((await params).id))).returning();return row?Response.json({ok:true}):Response.json({error:"资源不存在"},{status:404})}
