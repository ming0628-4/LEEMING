import{env}from"cloudflare:workers";import{getChatGPTUser}from"@/app/chatgpt-auth";
export async function POST(request:Request){if(!(await getChatGPTUser()))return Response.json({error:"需要管理员登录"},{status:401});const form=await request.formData();const file=form.get("file");if(!(file instanceof File))return Response.json({error:"缺少文件"},{status:400});if(file.size>500*1024*1024)return Response.json({error:"文件不能超过 500 MB"},{status:413});const key=`resources/${Date.now()}-${crypto.randomUUID()}-${file.name.replace(/[^a-zA-Z0-9._-]/g,"_")}`;await env.ARCHIVE.put(key,file.stream(),{httpMetadata:{contentType:file.type||"application/octet-stream"},customMetadata:{originalName:encodeURIComponent(file.name)}});return Response.json({key,name:file.name,size:file.size,type:file.type},{status:201})}
