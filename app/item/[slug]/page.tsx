import Link from "next/link"; import { notFound } from "next/navigation"; import { getResource, resources } from "@/lib/resources";
export function generateStaticParams() { return resources.map((r) => ({ slug: r.slug })); }
export default async function Item({ params }: { params: Promise<{ slug: string }> }) { const item = getResource((await params).slug); if (!item) notFound(); return <main className="shell item-page"><Link className="back" href="/archive">← 返回 Archive</Link><div className="item-grid"><section><div className="item-heading"><span className="file-badge large">{item.fileType}</span><div><p className="category">{item.category}</p><h1>{item.name}</h1><p>Version {item.version}</p></div></div><p className="item-description">{item.description}</p><div className="why"><p className="eyebrow">WHY I KEEP IT</p><h2>为什么保留</h2><p>{item.why}</p></div></section><aside className="item-aside"><dl><div><dt>文件大小</dt><dd>{item.size}</dd></div><div><dt>文件类型</dt><dd>{item.fileType}</dd></div><div><dt>最后更新</dt><dd>{item.updatedAt}</dd></div></dl><a className="primary-button" href={item.sourceUrl} target="_blank" rel="noreferrer">前往官方下载 ↗</a><p>当前版本使用官方来源；Personal Archive 将在 Sprint 3 接入。</p></aside></div><div className="tags">{item.tags.map((tag) => <span key={tag}>{tag}</span>)}</div></main>; }
