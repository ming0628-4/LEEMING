"use client";import{useState}from"react";import{useRouter}from"next/navigation";
type Info={fileName:string;fileSize:number;fileType:string;sha256:string};export function UploadForm(){const router=useRouter(),[selected,setSelected]=useState<File|null>(null),[info,setInfo]=useState<Info|null>(null),[busy,setBusy]=useState(false),[error,setError]=useState("");async function pick(f?:File){if(!f)return;setBusy(true);setSelected(f);const h=await crypto.subtle.digest("SHA-256",await f.arrayBuffer());setInfo({fileName:f.name,fileSize:f.size,fileType:f.name.split(".").pop()?.toUpperCase()||"FILE",sha256:[...new Uint8Array(h)].map(b=>b.toString(16).padStart(2,"0")).join("")});setBusy(false)}async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);try{let objectKey="";if(selected){const fd=new FormData();fd.set("file",selected);const r=await fetch("/api/files",{method:"POST",body:fd}),j=await r.json();if(!r.ok)throw new Error(j.error);objectKey=j.key}const data=Object.fromEntries(new FormData(e.currentTarget)),sourceUrl=String(data.sourceUrl||"")||(objectKey?`/api/files/${objectKey}`:"");const r=await fetch("/api/resources",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({...data,...info,objectKey,sourceUrl})}),j=await r.json();if(!r.ok)throw new Error(j.error);router.push("/admin")}catch(e){setError(e instanceof Error?e.message:"上传失败")}finally{setBusy(false)}}return <form className="upload-form" onSubmit={submit}><label className="dropzone"><input type="file" onChange={e=>pick(e.target.files?.[0])}/><strong>{busy?"正在处理…":info?.fileName||"拖入安装包，或点击选择"}</strong><span>{info?`${(info.fileSize/1048576).toFixed(2)} MB · ${info.fileType} · SHA256 ${info.sha256.slice(0,12)}…`:"自动读取文件信息与 SHA256"}</span></label><div className="form-grid"><label>资源名称<input name="name" required/></label><label>版本<input name="version"/></label><label>分类<input name="category" required/></label><label>标签<input name="tags"/></label></div><label>简介<input name="description"/></label><label>外部来源链接（上传文件时可留空）<input name="sourceUrl" type="url"/></label><label>为什么保留<textarea name="why" rows={5} required/></label>{error&&<p>{error}</p>}<button className="primary-button" disabled={busy}>{busy?"处理中…":"发布资源"}</button></form>}
